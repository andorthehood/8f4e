8f4e/v1

constants env
; @pos -84 14
; @favorite
; Auto-generated environment constants
; Changes will be overwritten

const SAMPLE_RATE 1

constantsEnd

entry main
module projectConfig
; @pos -84 2
; @config runtime WebWorkerRuntime
; @config workerRuntime.sampleRate 1
; @config export.fileName landingPageDemo2
; @config color.fill.background #096
; @config color.fill.backgroundDots #063
; @config color.fill.backgroundDots2 #063
; @config color.fill.wire #fff
; @config color.fill.moduleBackground #000
moduleEnd
module source
; @pos -1 7
; @home left

; Declare an integer
; with a default value:
int memory 14

moduleEnd
module relay
; @pos 36 2
int* pointer &source:memory
int memory

; The address stored in
; the pointer:
; @watch pointer

; The dereferenced value:
; @watch *pointer

; Push the address of
; the memory onto the stack:
push &memory
; Dereference the pointer
; and push the value onto
; the stack:
push *pointer
; The store instruction
; takes a value and an address
; from the stack, then stores
; the value at that address.
store

moduleEnd
module endpoint
; @pos 77 2
int* pointer &relay:memory

; The address stored in
; the pointer:
; @watch pointer

; The dereferenced value:
; @watch *pointer

moduleEnd
entryEnd