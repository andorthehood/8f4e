8f4e/v1

note groups
; @pos -46 -10
; @home
; This project demonstrates one level
; of project groups. The editor keeps
; each group's blocks together, while
; the compiler currently executes only
; blocks owned by the root project.

noteEnd

entry main
module projectConfig
; @pos -46 2
; @config runtime WebWorkerRuntime
; @config workerRuntime.sampleRate 10

moduleEnd

group counters
constants counterSettings
; @pos -8 -10

const int STEP 1

constantsEnd

module counter
; @pos -8 2

int value
; @watch value

push &value
push value
push STEP
add
store

moduleEnd
groupEnd

group indicators
module indicator
; @pos 25 2

int updates
; @watch updates

push &updates
push updates
push 1
add
store

moduleEnd
groupEnd
entryEnd
