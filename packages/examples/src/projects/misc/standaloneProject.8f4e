8f4e/v1

config project
; @pos -14 8
; ~sampleRate 1


scope "runtimeSettings"
scope "runtime"
set "WebWorkerLogicRuntime"

rescope disableAutoCompilation
set true 

configEnd

module counter
; @pos 25 8

int count
; @watch count

push &count
push count
push 1
add
store

moduleEnd
